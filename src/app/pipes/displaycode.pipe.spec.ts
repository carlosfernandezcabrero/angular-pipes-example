import { DisplaycodePipe } from './displaycode.pipe';

describe('DisplaycodePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaycodePipe();
    expect(pipe).toBeTruthy();
  });
});
